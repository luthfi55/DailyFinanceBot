# Update Agents

Update the AGENTS.md files to reflect changes made in this conversation.

Rules:
- Only update sections that actually changed in this chat. Do not rewrite unchanged sections.
- Do NOT use git diff or scan the entire codebase. Use only what was discussed/modified in this conversation history.
- If a package was modified, update that package's `packages/<name>/AGENTS.md`.
- If the change affects overall project structure or env vars, update root `AGENTS.md`.

Steps:
1. Read the relevant AGENTS.md file(s)
2. Apply only the specific updates matching this conversation's changes
3. If a new feature was added, update "Pending / Next Steps" to mark it done or add new items

Be concise. Write in English (matching the AGENTS.md language). Do not add fluff.
