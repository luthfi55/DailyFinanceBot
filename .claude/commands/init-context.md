# Init Context

Read the project documentation in `agents.md` to understand the full context of this project before starting work.

Steps:
1. Read `agents.md` in the project root
2. Summarize the key context: project overview, stack, monorepo structure, active models/schema, and pending next steps
3. Confirm you are ready to assist — mention 1-2 of the pending items from the "Pending / Next Steps" section so the user knows you've read it

Keep the summary concise (under 200 words). End with: "Ready to help — what would you like to work on?"
