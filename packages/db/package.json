{
  "name": "@finance/db",
  "version": "0.0.1",
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate && tsc && node -e \"const fs=require('fs'); fs.cpSync('src/generated','dist/generated',{recursive:true})\"",
    "db:push": "prisma db push",
    "studio": "prisma studio"
  },
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.14.0",
    "typescript": "^5.4.0"
  }
}
